import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "dark",
    background:{
      default:"#0E1016",
    },
    primary: {
      main: "#0E1016",
    },
  },
});

export default theme;
